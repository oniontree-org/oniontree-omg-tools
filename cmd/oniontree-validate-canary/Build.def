# Build for i386 architecture
#GOARCH=386
#GOOS=linux
#CGO_ENABLED=1

BUILDPATH=.
TARGET=$(BUILDPATH)/oniontree-validate-canary
LDFLAGS=-w -s
GCFLAGS=-trimpath=$(GOPATH)/src
ASMFLAGS=-trimpath=$(GOPATH)/src

